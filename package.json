{
  "name": "agent-view",
  "version": "0.1.0",
  "description": "A tool for visualizing and browsing AI Agent sessions.",
  "scripts": {
    "build": "bun run build.js",
    "dev": "bun run build && bunx --bun serve . -l 3000",
    "start": "bunx --bun serve . -l ${PORT:-3000}"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
